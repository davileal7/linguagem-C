#include <stdio.h>

void main(){
    int x, y ,soma = 0;
    for (y = 0; y < 10; y++) {
        printf("Digite um valor %d:",y);
        scanf("%d:",&x);
        soma = soma = x;

    }

    printf("Soma: %d", soma);
    
}